/** Markup padrão aplicado a todos os itens quando markup_override não é informado (45%). */
export const MARKUP_DEFAULT = 0.45

/** Valor mínimo aceito para markup_override (0% = custo direto, sem margem). */
export const MARKUP_MIN = 0

/** Valor máximo aceito para markup_override (500% = cinco vezes o custo). */
export const MARKUP_MAX = 5
