import { describe, it, expect } from 'vitest'

/**
 * Sanity test — valida que o ambiente Vitest está configurado corretamente.
 * Casos reais de pricing serão adicionados no PRP-002.
 */
describe('pricing module', () => {
  it('sanity: ambiente de testes funcional', () => {
    expect(true).toBe(true)
  })
})
